# Review UI Component

Review a component or page for architecture compliance, design system adherence, and complexity.

## Instructions

Use the `ui-reviewer` agent to review the following path: `$ARGUMENTS`

If no path is provided, ask the user which component or page they want reviewed.

Pass the full file or directory path to the agent and return its findings.
